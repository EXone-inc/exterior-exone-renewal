# CLAUDE.md — exterior-exone-renewal

エクステリア工房 EXone コーポレートサイト `exterior-exone.com` の**リニューアル開発用サイト**。テーマは自作の `wp-content/themes/exterior-exone`。環境情報（PHP / WP-CLI / MySQL ソケットのパス）は親フォルダの `Local Sites/CLAUDE.md` を参照。

## デザインカンプ（Figma）

- デザインの正は Figma「【作業用】コーポレートサイト」fileKey `msbO5SCyCUBUXQwXLUug4m`
- **カンプは 375px（SP）と 1920px（PC）の 2 幅のみ**。中間幅（タブレット等）のカンプは存在しない。中間幅は 2 幅のデザインを破綻しない範囲で自然に補間する
- フレーム対応表（下層ページのカンプが増えたらここに追記する）:

| ページ | PC | SP |
|---|---|---|
| TOP | `917:31` | `917:721` |
| COMPANY（企業情報） | `455:104`（PC 企業情報） | `494:511`（Frame 8） |

## デザイントークンの一元化

- 色・フォント・余白・角丸などのデザイン値は、Figma から **CSS カスタムプロパティ**に落とし、`style.css` の `:root` に 1 箇所で定義する
- **カンプの px 値やカラーコードをテンプレートや個別セレクタに直接散らさない**。必ずトークン（`var(--...)`）経由で使う
- 実装中にトークンにない値が出てきたら、先に `:root` へトークンとして追加してから使う（その場しのぎの直値を書かない）

## 「忠実再現」の定義

カンプとの一致判定は次の基準で行う:

- **一致必須**: セクション構成・配置・配色・タイポグラフィ・余白感
- **求めない**: px 単位の完全一致

この文言は `.claude/agents/evaluator.md` の「デザイン再現」軸と同一にしてある。**どちらかを変えるときは必ず両方を同時に更新する**（判定のブレを防ぐため）。

## 機能開発フロー

まとまった開発（新セクション実装・新ページ・CPT 追加など）は `.claude/agents/` の **planner → generator → evaluator** の 3 段で進める。各エージェントの入出力・規約・評価閾値は**各定義ファイルが正**であり、本ファイルには重複記載しない。仕様書は `docs/spec-*.md` に置かれる。バグ修正・文言修正・単発の CSS 調整は直接編集でよい。

## 判断に迷ったときは確認する

仕様・デザイン・実装方針で解釈が分かれる点が出てきたら、**推測で進めずその場で確認する**。黙って選んだ解釈が外れていると、後から作り直すコストのほうが高い。

- **必ず確認する**（勝手に決めない）
  - カンプに無い状態のデザイン … ホバー・選択中・エラー・0 件時・文字数超過時 など
  - アニメーションの速度・タイミング・きっかけ
  - カンプどうしが矛盾している、またはカンプの指定と実装上の都合が食い違うとき
  - 既に動いている実装を作り直す規模になるとき
  - 後述の「触らない領域」に手を入れる必要が出てきたとき
- **確認せず進めてよい**
  - カンプに答えが書いてあること
  - 命名・ファイル分割など、既存の規約から自明に決まること
  - バグ修正・文言修正・単発の CSS 調整
- **確認の仕方**
  - 選択肢を 2〜4 個示し、推奨案を先頭に置いて理由を 1 行添える
  - 答えを待つ間も、それに依存しない作業は進めておく
  - 溜めずにその都度出す。ただし同時に聞けるものは 1 回にまとめる

サブエージェント（planner / generator / evaluator）はユーザーに直接聞けない。**確認が要る点は勝手に決めず、仕様書やレポートに「要確認」として明記して返す**こと。呼び出した側がまとめてユーザーに確認する。

## 触らない領域

- `wp-admin/`・`wp-includes/`・トップレベル `wp-*.php` … WordPress 本体
- `wp-content/plugins/*` … プラグイン本体（有効/無効化のみ WP-CLI で可）
- `wp-content/uploads/` … メディア（追加が必要なら WP-CLI の `media import` で行う）

編集対象は実質 `wp-content/themes/exterior-exone/` のみ。
